# Local fixtures

Run `python fixtures/generate.py` to create a small native-text PDF and PNG OCR fixture.
Generated binaries are intentionally not committed; the test suite creates isolated fixtures too.
