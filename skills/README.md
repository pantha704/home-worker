# Runtime skills

These are application-level capability contracts, not autonomous workers. A skill may propose annotations, but the API workflow validates every output and owns all persistent state changes.

The default release enables deterministic local rules. Optional local models must pass the same schemas, policy checks, and regression cases before they can be enabled.

